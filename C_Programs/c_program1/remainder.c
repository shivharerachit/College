#include <stdio.h>
void main()
{
    int a,b,c;
    printf("Enter a no. to be divided:");
    scanf("%d",&a);
    printf("Enter a no. to divide with:");
    scanf("%d",&b);
    c=a%b;
    printf("Remainder is %d",c);

}
